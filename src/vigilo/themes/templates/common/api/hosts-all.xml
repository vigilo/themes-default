<?xml version="1.0"?>
<hosts xmlns="http://projet-vigilo.org/ns/api"
       xmlns:py="http://genshi.edgewall.org/"
       xmlns:xi="http://www.w3.org/2001/XInclude">

    <py:for each="host in hosts">
        <host id="${host.id}" href="${host.href}" name="${host.name}" />
    </py:for>

</hosts>
